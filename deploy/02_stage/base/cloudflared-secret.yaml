apiVersion: v1
kind: Secret
metadata:
  name: cloudflared-secret
  labels:
    app.kubernetes.io/name: hatshop
    app.kubernetes.io/instance: stage
    app.kubernetes.io/component: cloudflared
type: Opaque
stringData:
  # This will be replaced by SOPS or manually before deployment
  # DO NOT commit actual token to git
  CLOUD_FLARE_TOKEN: "REPLACE_WITH_ACTUAL_TOKEN"
